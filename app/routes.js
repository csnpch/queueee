const router = require('express').Router()

router.get('/processQueue', (req, res) => {
    res.redirect('/')
})

router.get('/displayQueue', (req, res) => {
    res.redirect('/')
})

module.exports = router